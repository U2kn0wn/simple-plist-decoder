# simple-plist-decoder
A Python tool to recursively find and decode .plist files (binary or XML) and print their contents in a readable format. Useful for mobile app analysis and reverse engineering.
